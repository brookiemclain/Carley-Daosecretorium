# DAO Secretorium

Next.js App Router project for the DAO Secretorium platform.  

## Setup
1. Copy `.env.example` → `.env` and add your secrets.
2. Run `npm install`
3. Run `npm run dev` to start the local server
4. Deploy via Vercel or let Blackbox Remote Agent handle commits & deployment
